#include <iostream>
#include <ostream>
#include <vector>
using std::vector;
using namespace std;

class Solution {
public:
  vector<int> twoSum(vector<int> &nums, int target) {
    for (int i; i < 10; i++) {
      cout << i << endl;
    }
    return {0, 0};
  }
};
